/**
 * Grip width image imports - currently no images exist, but structure is ready for future additions
 */
import type { ImageSourcePropType } from 'react-native';

export const GripWidthImages: Record<string, ImageSourcePropType> = {
  // Note: No grip width images exist yet, but this structure allows for future additions
  // Options: "Extra Narrow", "Narrow", "Shoulder Width", "Wide", "Extra Wide"
};
